def categories_by_slug(request, cat_slug):
    if request.GET:
        return HttpResponse("<h1>" + "|".join([f"{k} = {v}" for k, v in request.GET.items()]) + "</h1>")
    return HttpResponse(f"<h1> Статьи по категориям </h1> <p> <h2> slug: {cat_slug} </h2> </p>")


Кактакто?


Подвиг 5. Отметьте утверждения, справедливые для POST-запроса.

Выберите все подходящие ответы из списка
Верно решили 2 085 учащихся
Из всех попыток 24% верных
 Всё получилось!
Вы решили сложную задачу, поздравляем! Вы можете помочь остальным учащимся в комментариях, отвечая на их вопросы, или сравнить своё решение с другими на форуме решений.

x) данные POST-запроса не отображаются в URL-строке браузера

0) POST-запрос всегда срабатывает по умолчанию при отправке данных на сервер из HTML-форм в браузерах

x) POST-запрос срабатывает при передаче данных на сервер из HTML-формы при указании метода POST

x) POST-запрос эффективнее GET-запроса при передаче больших объемов данных (например, изображений)

x) POST-запрос следует использовать при передаче закрытых данных, например, логина и пароля


Подвиг 11. Объявите функцию представления с именем posts_list, которая связана со следующим маршрутом:

urlpatterns = [
    ...
    path('posts/<int:year>', posts_list),
]

                  
(Функция должна корректно срабатывать для него.) Внутри функции выполните проверку на корректность переданного года. Если он выходит за пределы [1990; 2023], то вызывать исключение 404 для отображения страницы с кодом 404. Если же год указан верно, то функция posts_list должна возвращать HTTP-ответ со строкой в формате:

"posts: <year>"

Например, для года 2023 должна возвращаться строка (в объекте HTTP-ответа):

"posts: 2023"

P.S. Функцию вызывать не нужно, только объявить.

from django.http import HttpResponse, HttpRequest, Http404

# здесь объявляйте функцию
def posts_list(request, year):
    if 1990 <= year and year <= 2023:
        return HttpResponse(f"posts: {year}")
    else:
        raise Http404


Нужно было просто написать без скобок в конце
